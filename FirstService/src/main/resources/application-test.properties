# Configuration of the application
server.port=8081
spring.application.name=animal-ms

# Connection data with H2 bank
spring.datasource.url=jdbc:h2:mem:db-animals
# Add
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.sql.init.mode=always
spring.aop.auto=true
spring.aop.proxy-target-class=true

# Configuration of the H2 bank web client
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Add
# spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.defer-datasource-initialization=true

# Configuration to show SQL in the console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# logging.level.org.springframework=DEBUG

# spring-datasource.url=jdbc:h2:file:/ebac/animais
# spring.jpa.hibernate.ddl-auto=none